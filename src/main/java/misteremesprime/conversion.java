/*package puntosClave;

public class conversion {
    public static void main(String[] args) {
        // Int --> String ---------------------------------------------------------------------------------------------
        int numero = 1234;

        // Convertir el número entero a String
        String numeroComoString = Integer.toString(numero);

        // String --> Int ---------------------------------------------------------------------------------------------
        String texto = "1234";

        // Convertir la cadena a un número entero
        int numero = Integer.parseInt(texto);

        // Double --> Int ---------------------------------------------------------------------------------------------
        double decimal = 12.56;
        int entero = (int) decimal;  // Conversión explícita (casting)
        System.out.println(entero); // 12

        // Char --> Int -----------------------------------------------------------------------------------------------
        char letra = 'A';
        int valorAscii = (int) letra;  // Conversión explícita
        System.out.println(valorAscii); // 65

        // Int --> Char -----------------------------------------------------------------------------------------------
        int numero = 66;
        char letra = (char) numero;  // Conversión explícita
        System.out.println(letra); // "B"
    }
}*/
